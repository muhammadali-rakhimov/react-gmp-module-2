import React from 'react'
import styled from './style.module.scss'

const index = () => (
  <button className={`${styled.searchButton} col col-md-3 p-3`} type="submit">
    SEARCH
  </button>
)

export default index
